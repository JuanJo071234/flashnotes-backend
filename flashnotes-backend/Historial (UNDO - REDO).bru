meta {
  name: Historial (UNDO / REDO)
  type: http
  seq: 4
}

get {
  url: http://localhost:5000/api/notes/:id/history
  body: none
  auth: inherit
}

params:path {
  id: 
}

settings {
  encodeUrl: true
  timeout: 0
}
